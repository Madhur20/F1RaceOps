# F1RaceOps — Python dependencies
# Install: pip install -r requirements.txt   (inside your venv)

# --- Web framework / API ---
fastapi>=0.111
uvicorn[standard]>=0.30

# --- Database ---
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9        # Postgres driver

# --- Data validation / config ---
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# --- F1 data ingestion ---
fastf1>=3.3

# --- Data processing / simulation ---
pandas>=2.2
numpy>=1.26
scipy>=1.13                 # distributions for Monte Carlo sampling
scikit-learn>=1.5           # tire degradation model (Phase 4 ML sub-step)

# --- Testing ---
pytest>=8.2
pytest-cov>=5.0
httpx>=0.27                 # FastAPI TestClient dependency

# --- Code quality ---
ruff>=0.5
mypy>=1.10
pre-commit>=3.7

# Not included yet — add when actually needed, not preemptively:
#   redis / celery       -> only once caching/background jobs become necessary (v2+)
#   ray                  -> only for parallel simulation (v2+)
#   sqlalchemy[asyncio]  -> only if you move to async endpoints